<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.humanid" >

    <application>

        <provider
            android:name=".provider.HumanIDInitProvider"
            android:authorities="${applicationId}.HumanIDInitProvider"
            android:exported="false"
            android:initOrder="100"/>

    </application>

</manifest>
